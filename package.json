{
  "name": "attainment-web",
  "version": "0.0.1",
  "description": "Website for the attainment app",
  "license": "BSD-3-Clause",
  "private": true,
  "devDependencies": {
    "del": "^2.2.1",
    "eslint": "^3.12.0",
    "eslint-config-google": "^0.7.1",
    "eslint-plugin-html": "^1.7.0"
  },
  "scripts": {
    "lint": "eslint . --ext js,html --ignore-path .gitignore",
    "test": "npm run lint && polymer test"
  }
}
